import { SeasonEpisodePipe } from './season-episode.pipe';

describe('SeasonEpisodePipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonEpisodePipe();
    expect(pipe).toBeTruthy();
  });
});
